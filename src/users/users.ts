import { id } from "./id"
import { name } from "./name"
import { recentActivity } from "./recentActivity"
import { scores } from "./scores"

export const users = {
    id,
    name,
    recentActivity,
    scores,
}
